package com.mycompany.promocalculator.test;

import org.testng.annotations.Test;

public class TestMainStart {
	private final org.slf4j.Logger logger = org.slf4j.LoggerFactory.getLogger(this.getClass());

	@Test
	public void testShopMain() {
		/*
		 * Shop shop = new Shop(); shop.main(new
		 * String[]{"src\\test\\resources\\pricelist.xml",
		 * "src\\test\\resources\\discount.xml",
		 * "src\\test\\resources\\invoices.xml" });
		 * 
		 * try {
		 * CommandFactory.getInstance().createCommand(CommandEnum.TEST.command
		 * ()).execute(shop.getContext(), new String[]{});
		 * CommandFactory.getInstance
		 * ().createCommand(CommandEnum.END.command()).
		 * execute(shop.getContext(), new String[]{}); } catch ( IOException |
		 * ParserConfigurationException | TransformerException e) {
		 * logger.error("Error getting file with command",e); }
		 */
	}
}
